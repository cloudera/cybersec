[cybersec]

setversion_cmd = ${MVN_CMD} ${MVN_SET_VERSION_CMD} -DnewVersion=${STACKVERSION}-${BUILDNUMBER}

    [[artifacts]]
        artifact_1 = flink-cyber/cyber-parcel/target/cybersec-${STACKVERSION}-${BUILDNUMBER}.parcel
        artifact_2 = flink-cyber/cyber-parcel/target/cybersec-${STACKVERSION}-${BUILDNUMBER}.sha1

    [[install_cmd]]
        cmd_1 = "${MVN_HOME}/bin/mvn -f ./flink-cyber/pom.xml -am clean install -T1C -DskipTests"
